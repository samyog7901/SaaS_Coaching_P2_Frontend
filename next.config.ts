import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: {
    buildActivity: false,
    buildActivityPosition: 'bottom-right', // or remove completely
    appIsrStatus: false,
  }
};

/** @type {import('next').NextConfig} */

  



export default nextConfig;
